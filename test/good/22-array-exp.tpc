int b, a[50], c[2];

char string[20], d;

int add(char a, char b) {
    return a + b;
}

int main(void) {
    int b, a[50], c[2], duration;
    int now, span;

    char string[20], d;

    string[0] = 'a';
    a[99] = 1;

    a[5] = duration[(now + span) % 12];

    return 0;
}
