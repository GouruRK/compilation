int tab[3];

int use_tab(int tab[]) {
    int a, b, c;
    a = 0;
    b = 1;
    c = 0;
    if (tab[0] == 0) {
        tab[a + b + c] = 3;
    }
    return tab[((1 + 2) * 0)];
}

int main(void) {
    tab[0] = 0;
    tab[1] = 1;
    tab[2] = 2;
    return use_tab(tab);
}